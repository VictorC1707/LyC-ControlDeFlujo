// Programa de prueba para el lexer L-Rust
fn calcular_total() {
    let mut contador = 0;
    let limite = 10;
    
    while contador < limite {
        contador = contador + 1;
    }
    
    if true {
        return contador;
    } else {
        return 0;
    }
}